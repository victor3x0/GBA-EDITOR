# Dépendances Python de l'éditeur — `pip install -r requirements.txt`.
#
# Ne lister ici que les paquets importés DIRECTEMENT par editor/ : les
# dépendances transitives sont tirées par pip toutes seules (antlr4 et
# multimethod pour luaparser, PyQt6-Qt6/PyQt6-sip pour PyQt6, qtpy pour
# qtawesome). `python packaging/check_deps.py` vérifie que cette liste
# couvre bien tous les imports du code — la CI le lance avant le build.
# Les bornes majeures empêchent qu'une mise à jour incompatible modifie une
# release sans changement du dépôt. Les bornes basses correspondent à
# l'environnement validé par la CI/release en septembre 2026.
PyQt6>=6.11,<7
Pillow>=12.3,<13
freetype-py>=2.5,<3
qtawesome>=1.4,<2
numpy>=2.5,<3
luaparser>=4.1,<5
